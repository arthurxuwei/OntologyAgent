services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: kovaloop-cloudflared
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN:?Set CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - ledger
      - circle
      - website
    restart: unless-stopped
